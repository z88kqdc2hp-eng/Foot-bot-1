name: Bot_Foot_Auto
on:
  schedule:
    - cron: '5 0 * * *' # Lance à 00h05 chaque jour
  workflow_dispatch:
jobs:
  analyse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python main.py
